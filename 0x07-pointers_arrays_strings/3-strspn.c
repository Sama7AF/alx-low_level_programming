#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strspn - new
 * @accept: var
 * @s: va
 * Return: o
 */

unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
